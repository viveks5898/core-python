# A tuple is list we can't change
fruits = ("apple", "mango")

numbers = (33,4,5,2,11,34)

print(numbers.count(33)) # we can use count methos when we have to check how many times the element occur in tuple


print(numbers.index(33)) ## check the index of  element in tuple