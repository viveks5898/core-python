Dynamic binding means varible can store multiple data types

static binding uses language like c , c++

 we can also declare same varible in one line 
 a=3;b=3 we just have to add semin colon between varibles
 a=b=c =6